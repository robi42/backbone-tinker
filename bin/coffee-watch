#!/usr/bin/env ringo

var fs = require('fs');

if (require.main === module) {
    require('ringo/subprocess').system(
            'coffee', '--output', fs.join('..', 'public', 'app'),
            '--lint', '--watch', fs.join('..', 'src', 'app'),
            {dir: module.directory});
}
